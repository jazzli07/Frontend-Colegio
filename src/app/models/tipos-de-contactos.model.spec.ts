import { TiposDeContactos } from './tipos-de-contactos.model';

describe('TiposDeContactos', () => {
  it('should create an instance', () => {
    expect(new TiposDeContactos()).toBeTruthy();
  });
});
